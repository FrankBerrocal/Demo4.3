<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/mi_settings"
        android:title="Settings"
        android:icon="@drawable/ic_settings_foreground"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/mi_contact"
        android:title="Add Contact"
        android:icon="@drawable/ic_add_contact_foreground"
        app:showAsAction="always"/>

    <item android:id="@+id/mi_favorites"
        android:title="Favorites"

        app:showAsAction="never"/>

    <item android:id="@+id/mi_feedbacks"
        android:title="Feedback"
        android:icon="@drawable/ic_settings_background"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/mi_close"
        android:title="Close"
        android:icon="@drawable/ic_settings_background"
        app:showAsAction="ifRoom"/>

</menu>